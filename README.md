# Linux-Bcache-Monitor

## Installation (einfach)

Dieses `curl` lädt die Datei in den **aktuellen Pfad** und macht sie direkt ausführbar:

```bash
curl -fsSL https://raw.githubusercontent.com/<USER>/<REPO>/main/bcache-monitor -o ./bcache-monitor && chmod +x ./bcache-monitor
```

Danach starten mit:

```bash
./bcache-monitor
```
